%p#notice= notice
%h1 Books
%table
  %thead
    %tr
      %th Cover
      %th Title
      %th Date of publication
      %th Genre
      %th Annotation
      %th Volume
      %th Language
      %th{:colspan => "3"}
  %tbody
    - @books.each do |book|
      %tr
        %td= image_tag(book.cover_url(:thumb).to_s)
        %td= book.title
        %td= book.date_of_publication
        %td= book.genres.collect{|g| g.genre_name}.join(", ")
        %td= book.annotation
        %td= book.volume
        %td= book.language
        %td= link_to 'Show', book
        %td= link_to 'Edit', edit_book_path(book)
        %td= link_to 'Destroy', book, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Book', new_book_path
