var controllerName = "<%= @book.class.name.downcase %>"
$.each (<%= raw @book.query.to_json %>, function(i, val){
    $(".hide_field#" + val).remove()
    $(".field#" + val).removeClass("hidden")
    $("#" + controllerName + "_" + val).enableClientSideValidations()
})
