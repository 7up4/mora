%p#notice= notice
%h1 Authors
- @authors.each do |author|
  .author.flex-container
    .left-container
      = image_tag(author.photo_url(:thumb).to_s, class: "shadowed")
    .right-container
      .header
        = author_fullname(author.first_name, author.second_name,author.last_name)
        .actions.btn-group
          = link_to 'Show', author, class: 'btn btn-success btn-responsive'
          = link_to 'Edit', edit_author_path(author), class: 'btn btn-success btn-responsive'
          = link_to 'Destroy', author, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger btn-responsive'
      .content
        = author.biography
%br/
= link_to 'New Author', new_author_path
